% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/R_boxplot.R
\name{R_boxplot}
\alias{R_boxplot}
\title{R_boxplot: Boxplot with Scatter and a/b/c Letter Significance}
\usage{
R_boxplot(
  data,
  class_col = "class",
  feature_cols = NULL,
  point_alpha = 0.5,
  point_size = 1.2,
  box_width = 0.5,
  box_alpha = 0.6,
  letter_size = 4,
  letter_ymin_frac = 0.05,
  ylab = NULL
)
}
\arguments{
\item{data}{A data frame: rows = samples, columns = features, last column =
\code{class} (grouping). All columns except \code{class} are plotted.}

\item{class_col}{Character. Name of the grouping column. Default \code{"class"}.}

\item{feature_cols}{Character vector. Which columns to plot as y. Default
\code{NULL} = all columns except \code{class_col}.}

\item{point_alpha}{Numeric. Point transparency. Default \code{0.5}.}

\item{point_size}{Numeric. Point size. Default \code{1.2}.}

\item{box_width}{Numeric. Box width. Default \code{0.5}.}

\item{box_alpha}{Numeric. Box fill transparency. Default \code{0.6}.}

\item{letter_size}{Numeric. Significance letter text size. Default \code{4}.}

\item{letter_ymin_frac}{Numeric. Letter y position as fraction of y range
above each group max. Default \code{0.05}.}

\item{ylab}{Character. Y-axis label. Default \code{"value"}.}
}
\value{
A \code{ggplot} object (boxplot + points, no jitter, with letters).
}
\description{
Draws boxplots with overlaid points (no jitter) and Tukey HSD letter
markers (a, b, c, ab, ...) for group comparisons. Input is in "universal"
format: each row = sample, each column = feature, last column = \code{class}.
}
\examples{
set.seed(1)
dat <- data.frame(
  gene1 = c(rnorm(20), rnorm(20, 1), rnorm(20, -0.5)),
  gene2 = c(rnorm(20, 2), rnorm(20), rnorm(20, 0.5)),
  class = rep(c("A", "B", "C"), each = 20)
)
R_boxplot(dat)
}
